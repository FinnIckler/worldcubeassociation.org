<% provide(:title, 'WCA Live Integration Test Page') %>

<%= render layout: 'competitions/nav' do %>
  <%= react_component('Live/TestResultPage', { competitionId: @competition_id, competitors: @competitors.as_json({ include: [:user => { only: [:name], methods: [], include: []}]}), roundId: @round.id, eventId: @event_id }) %>
<% end %>
