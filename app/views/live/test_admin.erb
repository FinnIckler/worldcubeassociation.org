<% provide(:title, 'WCA Live Integration Test Page') %>

<%= react_component('Live/TestAdminPage', { competitionId: @competition_id, competitors: @competitors.as_json({ include: [:user => { only: [:name], methods: [], include: []}]}), roundId: @round.id, eventId: @event_id }) %>
