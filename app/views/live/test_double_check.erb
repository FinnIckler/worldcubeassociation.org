<% provide(:title, 'WCA Live Integration Test Page') %>

<%= render layout: 'competitions/nav' do %>
  <%= react_component('Live/TestDoubleCheckPage', { competitors: @competitors.as_json({ methods: [:name, :user]}), round: @round.as_json( { include: [:event], only: [:id], methods: [:name]}), competitionId: @competition.id, }) %>
<% end %>
