<% provide(:title, "WCA Live #{@competition_id} Schedule") %>
<%= render layout: 'competitions/nav' do %>
  <%= react_component('Live/TestScheduleAdminPage', { competitionId: @competition_id, rounds: @rounds.as_json({ methods: [:event, :round_type, :name]}) }) %>
<% end %>
