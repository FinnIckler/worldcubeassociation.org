<% provide(:title, 'WCA Live Integration Test Page') %>

<%= react_component('Live/TestSchedulePage', { competitionId: @competition_id, rounds: @rounds.as_json({ methods: [:event, :round_type, :name]}) }) %>
